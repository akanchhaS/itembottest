/*
sendsms -> sends an sms as an alert
sendemail -> sends an email as an alert
itemfile -> defines the file that contains a newline separated list
sleeptime -> how frequently to scan the page in milli seconds
*/
module.exports = {
    sendsms: 'yes',
    sendemail: 'yes',
    itemfile: 'item.txt',
    sleeptime: '30000'
}
